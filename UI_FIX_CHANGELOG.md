# Safe Space UI 修正版 2 — 測試回饋修正

本版針對實機/瀏覽器測試回饋處理：

1. 塔羅圖書館切換時，隱藏主閱讀舞台，避免兩個 flex 區域同時分割高度，造成圖鑑內容只剩一小條。
2. 塔羅圖書館保留「內部捲動」，主塔羅閱讀流程不再整頁捲動。
3. 移除元件 CSS 中最後一段會重新開啟 `#feature-content-area` 整頁滾動的衝突規則。
4. 解答之書的「點擊書本翻頁」改為垂直排列在書本正下方。
5. 解答之書維持 3D overflow，不使用整頁捲動，避免滑動造成 3D 書本被裁切。
6. DBT 選卡包改用 Pointer Events，支援滑鼠與手機觸控拖曳，並阻止觸控時被瀏覽器手勢接管。
7. DBT 撕卡包改用 Pointer Events + pointer capture，觸控滑出小範圍後仍可繼續拖曳。
8. DBT 撕卡包的透明觸控區由 70px 提高至 90px，提升手機命中率。
9. Tarot navigation 內部修正 `libraryEl` 變數引用錯誤。
10. Router 關閉功能時會真正執行 feature cleanup，再清除引用。

靜態驗證：
- HTML/CSS/JS 路徑檢查通過
- JavaScript 語法檢查通過

## v3 — 手機版解答之書開書連接修正
- 修正手機開書後「左側書脊」與「右側答案頁」分離的視覺破圖。
- 開書狀態下將書脊改為貼在內頁左緣的窄版中央書縫，並隱藏原本的直排書脊文字。
- 保留關書狀態原本的 3D 書脊效果；只有手機且已開書時套用此修正。


## v4 — 手機版解答之書開書連接與閃爍修正

- 僅修改手機 `max-width: 700px` 的「已開書」狀態。
- 開書後取消書脊 `rotateY(-90deg)`，改為穩定的平面裝訂條，避免不同裝置 GPU 合成造成白塊／閃爍。
- 內頁在開書狀態改為平面層，左緣直接貼住書脊。
- 增加細微書縫視覺，讓書脊與內頁連成一體。
- 隱藏開書後的右側 3D 紙邊，避免產生額外 3D 合成層。
- 關書狀態、桌面版、答案內容與翻頁邏輯不更動。
